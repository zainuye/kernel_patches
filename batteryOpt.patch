--- a/init/Kconfig
+++ b/init/Kconfig
@@ -1,5 +1,24 @@
 menu "General setup"
 
+config BATTERY_SAVER_OPTIMIZATIONS
+	bool "Enable Aggressive Battery Optimizations"
+	default y
+	select WQ_POWER_EFFICIENT_DEFAULT
+	select ARM_CPUIDLE
+	select CPU_FREQ_DEFAULT_GOV_SCHEDUTIL
+	select ANDROID_PARANOID_NETWORK
+	help
+	  Forces energy-efficient workqueues, enables deep sleep C-states,
+	  sets Schedutil as the primary governor, and strips out 
+	  heavy debugging overhead to improve battery life.
+
+config DISABLE_DEBUG_FOR_BATTERY
+	bool "Disable Heavy Debugging"
+	default y
+	depends on BATTERY_SAVER_OPTIMIZATIONS
+	help
+	  Specifically targets FTRACE, KGDB, and STACK_TRACER to reduce CPU wakeups.
+
 config BROKEN
 	bool
  
